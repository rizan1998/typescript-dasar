"use strict";
describe("Data Type", function () {
    it("should must declare", function () {
        const name = "Rijan Alpalah";
        const balance = 10000;
        const isVip = true;
        console.log(name);
        console.log(balance);
        console.log(isVip);
    });
});
